name: Build war file And Deploy to Azure Cloud

env:
  AZURE_WEBAPP_NAME: javawebapp
  JAVA_VERSION: '11'
  DISTRIBUTION: zulu

on:
  push:
    branches:
      - main
  workflow_dispatch: 

permissions: 
  contents: read
  packages: write
  

jobs:
  Build-War-File:
    runs-on: ubuntu-latest
    environment:  production
    
  

    steps:
      - name: Checkout self
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
           java-version: ${{env.JAVA_VERSION}}
           distribution: ${{env.DISTRIBUTION}}
           cache: 'maven'
     
      - name: Build a Source Code
        if: always()
        run: mvn clean install -f MyWebApp/pom.xml
        continue-on-error: true
      - name: Build a Test
        if: always()
        run: mvn clean test -f MyWebApp/pom.xml
        continue-on-error: true
   
    
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: MyWebApp
          path: ${{ github.workspace}}/MyWebApp/target/*.war

  deploy:
     permissions:
      contents: read
     runs-on: ubuntu-latest
     needs: Build-War-File
     environment: 
      name: production
      url:  ${{ steps.deploy-to-webapp.outputs.webapp-url }}
      
     steps:
       - name: Download Artifacts
         uses: actions/download-artifact@v4
         with:
            name: MyWebApp
       - name: Deploy to Azure Web App
         id: deploy-to-webapp
         uses: azure/webapps-deploy@v3.0.6
         with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: '*.war'
     
          
    
